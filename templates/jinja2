<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Question Answering</title>
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

    <script type = "text/javascript"  src = "{{ url_for('static', filename='index.js') }}">
</script>

    <script>
        {% if kq %}
            var sens = {{kq|safe}};
            var btns = "";
            var tag = "";
            var i=0;
            for(sen in sens) {
                tag = "<button type=button onclick=getAns(" +sen+ ") class=btn btn-primary> Câu trả lời" + i +" </button>";
                i++;
                btns += tag;
            }
            console.log(btns);
            document.getElementById("btn_ans").innerHTML = btns;
            //$("#btn_ans").html(btns);


        {% endif %}



    </script>

</head>

<body>
      <div>

      <form action = "http://127.0.0.1:8008/qa" method = "post">
          <p>Question:</p>
         <p><input type = "text" name = "nm" style=" width:500px; height:100px " ></p>
         <p><input type = "submit" value = "Get answer" ></p>

      </form>



      {% if kq %}
          <div>
          <button> Send to teacher </button><br>
          {% if kq == "none" %}
                <h2> Hệ thống chưa trả lời được . Vui lòng gửi câu hỏi trực tiếp tới Giáo viên </h2>
          {% else %}
              <div class="btn-group" id="btn_ans">

              </div>
              <div class="well"></div>

          {% endif %}
          </div>
      {% else %}
      <h1> Answer</h1>
      {% endif %}

      </div>

</body>
</html>